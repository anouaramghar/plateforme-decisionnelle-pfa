# Copy to .env and fill in a real key. Never commit .env.
# Get an Anthropic key at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-YOUR_KEY_HERE
PORT=4000

# Required for verifying authorization tokens (JWT). Must match backend's JWT_SECRET.
JWT_SECRET=
